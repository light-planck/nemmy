pre-commit:
  parallel: true
  commands:
    frontend-linter:
      root: "frontend/"
      glob: "*.{js,jsx,ts,tsx}"
      run: bun next lint --fix --file {staged_files}
    frontend-style:
      root: "frontend/"
      run: bun prettier --write --ignore-unknown {staged_files}
